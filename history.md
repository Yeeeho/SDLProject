6/14
ui프레임 안에 알아서 들어가도록,
문자당 텍스트를 하나씩 렌더링하게 만들었더니 성능 과부하 씨게옴.

그래서 입력된 텍스트를 통째로 렌더링하고
그걸 클리핑하는 방식으로 바꿨다.

GPU 를 18퍼센트나 처먹던걸 6퍼센트대로 내렸다. 집컴기준일거임

6/15~

근데 그런식으로 잘라내서 붙이는 방식은
프레임에 맞춰서 이쁘게 렌더링하기 존나 어려웠다.

6/17
따라서 처음에 만들었던 1문자=1텍스처 방식을 다시 되살려서
업데이트가 있을 때마다 한 텍스처에 그려놓고
업데이트가 없을 경우엔 그 그려진 텍스처 하나를 렌더링 하는 방식으로
고치게 되었다..

작업 관리자 보니까 gpu 오버헤드는 이게 더 심한데
어쩌라고 너무 편하노 이거 쓸거임 ㅋㅋ

근데 씨발 임시 텍스처를 한번 거쳐서 렌더링하는 방식을 쓰니까 텍스처가 살짝 흐려졌다.
알고보니 임시 텍스처에 블렌딩 옵션을 킨 채로 투명한 배경에 렌더링하면, 임시 텍스처의 픽셀 값이 깎여나가는 거였다.
임시 텍스처와 타겟 텍스처 둘다 블렌드 모드가 켜져 있어서 알파가 원치 않게 합성되는 거라고 보면 되겠다.
픽셀 값을 그대로 유지하려면 블렌드 모드를 꺼야 한다.
임시 텍스처 = 블렌드 모드 없음 설정 <--- 이거 기억해라..

6/25
엔티티 객체를 생성하고 삭제하는 것은 분명 메모리 최적화 관점에서는 좋은 방법이지만,
코드 구현이 어려워지고, 생성 삭제시 cpu 부하가 늘 것이다. 
그래서 널 객체를 여러개 생성해두고 상황에 따라 바꾸는 방식을 쓰기로 했다.

7/5
문제점: 맵타일 툴팁을 만들었는데 느리게 움직이면 존나 깜박이고 빨리 움직이면 툴팁이 그냥 안생김
정지했을 때만 제대로 보임
내 생각에는 이벤트 핸들링이나 렌더링 함수를 원래 ui클래스에서 쓰던 걸 가져와서
실시간으로 움직이는 툴팁에 쓸려니까 문제가 생긴거 같음.

문제 원인: 임시 텍스처에 베이킹하는 로직을 여기에 쓰는게 문제였음. 정적인 ui들은 업데이트 플래그가 참일 때마다
업데이트 하는 방식이 유효하지만, 툴팁은 어차피 마우스가 조금만 움직여도 새로 렌더링을 해야 함. 내가 마우스가 ui위에 있을 때마다
업데이트를 하게 만들어버렸더니 실시간으로 텍스트 베이킹을 하고 있어서 렌더링이 끊기는거 같음.
만약 최적화를 하고 싶다면 마우스가 움직이지 않을때 임시 텍스처를 렌더링하는 방식을 써야할거 같은데 이거는 오바고.

해결 방법:
1. 그냥 임시 텍스처를 거치지 않고 쓰는 방법이 있다. 즉, 텍스트 아틀라스를 구현해야된다.
끼얏호우!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
2. 아니면 마우스 따라 움직이게 하지 말고 특정 위치에 고정되어서 생기게 바꾸는 방법도 있다.
이게 차라리 나을지도 모르겠다..

7/6
고정형 툴팁으로 바꿨다. 그런데 새로운 문제 발생

문제점: 다른 맵타일로 옮겨갈때 가끔 툴팁이 예전 자리에 그대로 머물러 있는 현상 발생.

원인: 
아마도 반복문으로 타일 위치를 검색하는 로직이 문제인거같음. 반복문을 안써야할듯함.

해결:
맵의 시작 좌표와 마우스 좌표 사이의 거리를 구한 다음 타일 길이로 나누어 직접
타일의 좌표를 구하고 그걸로 아이디를 구하도록 메서드를 만들었다.
함수명은 whattileonmouse()

7/7
문제의 연장:
그래도 문제생김
로그를 겁나 넣어서 디버깅을 해보니까
좌표를 설정해주는 로직에 문제가 있는거같다.

렌더링 함수 자체에서 타일 위치를 검사하는 방법이면 될거같기도 하다.

원인:
기존 함수는..
업데이트 플래그가 참일 때는 툴팁의 좌표 변수를 찾아서 렌더링을 잘 하지만,
거짓일 때는 그냥 구워둔 텍스처만 렌더링한다.
만약에 렌더링 함수가 돌아가는 도중에 유저가 마우스를 옮겨서 마우스 좌표 변수가 변경되었을때,
업데이트 플래그가 바뀌지 않는 상황이 발생할 수도 있다. 이런 상황에서 기존 로직을 사용하면,
원래 있던 자리에 그냥 고대로 렌더링하는 참사가 발생하는 것이다.

해결:
이런 상황을 막기 위해.. 업데이트를 하고 난 뒤, 평소 상태에서도 툴팁의 좌표 변수를
조회하도록 코드를 수정했다. 

문제2:
근데 내용은 어차피 업데이트가 안되더라.
당연히 그럴수밖에 없지. 렌더링 도중에 마우스를 옮겨버리면 업데이트를
검사 못하니까

해결:
업데이트 로직에서 실시간으로 검사를 때려주었다. 그리고 업데이트 시의 로직 또한 이 함수에다 넣어주었다.

7/10
'도시 맵' 클래스가 따로 있는 건 너무 비효율적이라고 생각했다.
그래서 맵 클래스를 재사용하게 변경했다.

7/15
시나리오 진행 데이터를 json에다 분리시켜서
데이터 중심적으로 설계했다.

7/19
코드 진짜 개쓰레기같네 ㅋㅋ
코딩은 구아노 쌓기다.

7/20
ccw 알고리즘을 사용해서 직선이 사각형에 충돌하는지 알 수 있다.
이걸로 원거리 공격 로직을 만들 수 있다.
근데 생각해보면 어짜피 타일셋 기반이잖아.
ccw를 쓰면 선분에 충돌하는 모든 타일을 감지해버려서 범위 표현이 부자연스럽다.
그러니까 그냥 단순히 최대거리/최소거리 이런식으로 계산하는게 나을듯
MapManager 의 타일 사이의 타일 구하는 함수에 구현했다.

7/21
아 시발 찾아보니까 브레젠험 알고리즘이라는게 또 있다.
어떤 사람이 사분면에서 동일하게 작동하는 직선 레스터라이징(벡터 그래픽을 픽셀로 바꿈) 함수를 만들어놔서
그걸 사용하여 원거리 범위 표시 기능을 구현했다.

7/22
실수를 통해 알아낸 건데, SetRenderPresentation 으로 논리적 해상도 기준을
현재 해상도보다 작게 잡으면, 렌더러가 렌더링하는 것들이 붕 떠 보여서 
원근감을 준다. 이거 써먹을 데가 어딘가 있지 않을까

7/23
데이터 은닉화를 하면 아무 생각 없이 함수만 써서 스탯을 가져오면 되는데
은닉화를 안하면 스탯연산이 필요한 수치는 함수 써야 하고 어떤거는 그냥 곧바로 접근하고 하니까 좀 불규칙적이긴 하네

아이템 클래스를 분리했다. enum 클래스들 모아놓은 거랑, 아이템 데이터를 다루는 클래스랑, 매니저 클래스로
어두운 미래를 위해서..

7/30
스킬의 데이터베이스를 분리했다. 개발하기 편하게 할려고. 근데 이런식으로 db를 분리하면
스킬 코드를 받았을때 어느 db에 해당하는지 따로 매핑하기 위한 db가 필요해져서.. 
db는 어지간하면 하나로 관리하는 게 좋은 듯 하다.
 
 8/1
 원래는 렌더링 함수 내부에 임시 텍스처에 베이킹하고 렌더링하는 동작을 둘다 넣었었는데, 그렇게 하니까
 렌더링이 끊기는 현상이 발생했다. 그래서 베이킹과 실제 렌더링 동작을 분리했고, 렌더링 함수에서는
 먼저 렌더링 동작 후, 텍스처 베이킹을 하게 만들었다. 그 결과 렌더링이 끊기지 않았다.

 8/4
 드디어 텍스트 ui를 제대로 고쳤다 ㅅㅂ

 8/7
 함수에서 리턴 빼먹으니까 널포인터 접근할때 발생하던 
 정의되지 않은 행동 에러가 나서 터미널이 그냥 꺼져버린다..

 8/11
 아이템 매니저에서 아이템에 관련된 모든것을 처리하려고 했는데,
 맵에 존재하는 아이템들까지 같이 관리하려다 보니 코드가 너무 더러워져서
 그냥 맵 객체가 아이템 스택들을 관리하게 만들었다.
 책임 분리 측면에서는 안좋은거 같지만 코드 위생과 이성 보존 측면에서는 좋은 것 같다.

 8/19
 렌더링 메서드는 반드시 매개변수로 좌표를 받을 수 있게 만들자
 상대좌표를 계산해야 할 경우가 있으니까..

 이벤트 핸들링 시 하나의 객체를 핸들링한 뒤 곧바로 다음 객체를 핸들링하는 현상이 발생했다.
 예시로 팝업창이 사라진 뒤 곧바로 팝업창 뒤에 있던 객체를 핸들링 하는 문제가 있다.
 매니저 클래스에 actionperformed 플래그 변수를 추가하여 이벤트 핸들링이 한번 발생했다면
 더이상 핸들링을 하지 않도록 방어할 수 있겠는데..

 그것보다는 투명 레이어를 팝업 뒤에 한번 더 띄워서 그게 핸들링 되는 순간 이벤트 전파를
 막는 방식이 더 낫다고 한다.

 아니면 핸들링 했을 때 바로 이벤트 루프를 다시 돌리는 방식을 쓰던가..

 8/22
 제일 좋은 방법은 이벤트 객체에 애초에 핸들링 되었는지 유무를 판별하는 데이터 필드를 추가해주는 건데 이미 그러기엔 늦어서 그냥 컨텍스트 객체가 정보를 가지고 있게 만들려고 한다..

 다음부터는 이벤트 핸들링 로직 만들때 꼭 전파 방지를 생각하자 슈발럼아

 8/30
 새로운 데이터가 추가되면 바로 세이브/로드 동작 또한 같이 만들어 주는게
 코딩하기 편한 거 같다.

 9/1
 적의 행동을 미리 보여주고 그에 대응하는 것을 플레이 방식으로 선택하였다.
 그 결과 엔티티마다 큐에 스킬 컨텍스트를 넣고
 유저가 컨텍스트를 ui를 통해 보고, 유저 턴이 끝나면 컨텍스트대로 스킬이 실행되는 구조가 되었고
 겁나 복잡해졌다.