# TODO

했으면 changelog에다가 기재할것

Done:
- 텍스트 렌더링 최적화
- json api
- 맵 이미지 기반으로 다시 고치기
- 엔티티/ 팀 생성-삭제 동작

- 현재 수정할거:
    - 아이템 툴팁에서 아이템 스택시 몇개 더있는지 표시
    - 줍기 동작 등을 액션 클래스를 새로 만들어서 거기로 옮기기: maybe?
    - ui 이벤트 전파 방지

-README:
    - 프로젝트 아키텍처 설명 첨부: in progress

- 리팩토링
    - 트랜스폼 객체 비슷한걸 도입?
    - 맵이 가지는 스폰된 엔티티 컨테이너를 현재 벡터 형식이 아니라
    맵 형식으로 바꿔서 엔티티 객체 접근 속도를 높이기: maybe

- 시스템:
    - 랜덤 클래스 구현: not yet
    - 실수, 정수, 부울 랜더마이저를 구분: not yet

- 그래픽:
    - 디버그용 엔티티 스프라이트: done
    - 히트 스프라이트 구현: in progress

- 그래픽 최적화:
    - 스프라이트 시트 구현: done
    - 스프라이트 시트에서 스프라이트를 찾아오는 동작: in progress
    - 맵 타일이 각자 텍스처 객체를 갖는걸 수정: not yet
    - 정확히 말하면 플라이웨이트 디자인 패턴을 씀.

- 데이터
    - 유저 데이터 세이브/로드: in progress

- 시나리오:
    - 시나리오 클래스 구현: done
    - 동작: 
        - 조건에 충족되면 다음 시나리오로 넘어가는 옵저버 패턴 구현: in progress
    - 데이터:
        - 시나리오 데이터 로딩 동작: done
        - 인트로 부분 구현: in progress
        - 메인 시나리오, 서브 시나리오 구분: in progress

- 맵
    - 수정사항:
        - 맵 객체가 엔티티 컨테이너를 가지지 않게 바꿔야함(책임 분리): in progress

    - 카메라 구현: done
    - 줌인 줌아웃 구현: maybe?

    - 아이템:
        - 아이템 스택 구현: done
        - 아이템 맵에 스폰시 ui에도 스택 데이터를 로드함: in progress
        - 아이템 다수 스택시 생략 그래픽 구현: in progress

    - 서브 맵:
        - 전투/탐험용 서브맵 구현: done
        - 서브맵 지형 구현: in progress
        - 오버맵으로 이동 기능 구현: in progress

    - 기본 ui
        - 맵 툴팁 구현: done
        - 맵 객체에 존재하는 다른 정보 로드: done
        - 타일 객체의 정보 컨테이너 클린업 기능: done

- 턴 시스템:
    - 일반:
        - 서브맵 턴 구현: done
        - 글로벌 턴 동작: not yet
    - ui:
        - 턴 종료 버튼 구현: done
        - 턴 종료 버튼 기능 완성: done

- 전투 시스템:
    - 엔티티 포커스 구현: done
    - 근접전 시스템 구현: done
    - 엄폐 시스템 구현: in progress
    - 원거리전 시스템 구현: in progress
    - 범위 공격 구현: in progress
    - 엔티티 사망 동작 구현: done

- 스킬:
    - 스킬 데이터 구현: done
    - 스킬 로직 구현: done
    - 타겟 설정: done
    - 멀티 타겟 스킬 구현: maybe?
    - 광역 스킬 구현: yet

    - 엔티티 연결:
        - 엔티티에 실제로 연결: done
        - 스킬 코스트에 따라 엔티티 스탯에서 차감: done
    
    - ui:
        - ui에도 표시: done
        - 공격(이동) 범위 표시 구현: done
        - 기술의 실제 범위에 따라 범위 표시 제한: done

- 엔티티:
    - 수정사항:
        - 우클릭으로 공격 및 이동: maybe
    - 일반:
        - 엔티티에게 턴당 hp, sp, ap 재생 수치: done

    - npc:
        - 드롭테이블 구현: ny

    - 데이터:
        - 엔티티 데이터를 개별 엔티티로 쪼개지 말고 카테고리로만 구현하고 실제 로직에서 플레이어 레벨에 맞게 스케일링하여 엔티티 객체 구현: in progress

    - 인벤토리:
        - 아이템 줍기 동작 구현: done        
        - 아이템 버리기 동작 구현: in progress
        - 아이템 서브 ui 구현: done
        - 장비:
            - 장비 장착/해제 동작: done
            - ui에 반영: done

    - 맵 이동 (스킬로 편입된):
        - 엔티티 이동시 연관 데이터도 움직이게 바꿈: done
        - 엔티티 이동시 행동력을 소모하게 바꿈: done 
        - 엔티티 데이터에서 hp 같은 스탯을 그대로 가져오지 않고, str 등과 같은 스탯을 가져와서 로직에 따라 실스탯을 결정하게 만듬: done
    
    - 스킬:

    - ui:
        - 엔티티 정보 확인 팝업 구현(캐릭터 시트): in progress
        - 체력바, ap바 등 간략화된 엔티티 정보 ui 구현: done
        - 인벤토리 ui 기본적 구현: in progress

    - pc(플레이어블 캐릭터) 특성 클래스 : not yet

- 아이템:
    - 소모품:
        - 데이터베이스 구현: in progress
        - 인게임에서 동작: in progress

    - 맵:
        - 맵에서 스폰되면 렌더링됨: done
        - 한 타일에 스택해서 스폰: in progress

    - 장비:
        - 장비 클래스 구현: done
        - 장비 데이터베이스 구현: done
        - 전투 시스템에 장비 데이터 반영: yet
        - ui에 엔티티 장비 상태 표시: yet

- 도시 기본 ui
    - ui 클래스와 맵 클래스 연결(ui manager): done
    - 툴팁: done

- 텍스트
    - 스스로의 길이를 구하는 메서드: done

- 범용 ui
    - 프레임 텍스트 ui:
        - 프레임 넓이에 텍스트가 다 차면 단어 단위로 옮기지 말고 글자 단위로 쪼개서 옮겨야됨
    - 텍스트 아틀라스 구현: maybe
    - 툴팁
        - 툴팁 동적 렌더링: done
        - 툴팁 깜박임 해결: done
        - ui매니저 툴팁 의존성 개선: in progress

    - 로그 ui:
        - 기본적 구현 : done
        - 텍스트가 다 차면 로그창 초기화: done
        - 초과된 줄의 넓이만큼 텍스트를 올림(여유가 되면..): maybe
  
    - esc 메뉴:
        - 구현: in progress

    - ui스택:
        - 구현 : done


- 대화 ui
    - 비주얼 구현: done
    - 기본 작동: done



